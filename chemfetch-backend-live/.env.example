// server/.env.example

# Supabase project URL (e.g. https://yourproject.supabase.co)
SB_URL=

# Supabase service role key (server-side only — do NOT expose to frontend)
SB_SERVICE_KEY=

# Optional: override port (default is 3000)
PORT=3000
