# Lightweight requirements for memory-constrained deployment
# Core Flask and web services
Flask==2.3.3
flask-cors==4.0.0
requests==2.31.0

# Lightweight PDF processing
PyMuPDF==1.23.5  # fitz - used in sds_extractor.py
pdfminer.six==20231228
pdf2image==1.16.3

# Lightweight OCR (instead of heavy PaddleOCR)
pytesseract==0.3.10
Pillow==10.0.1

# Basic image processing
opencv-python-headless==4.8.1.78  # Headless version saves memory
numpy==1.24.4

# Text processing and parsing
python-dateutil==2.8.2

# Remove heavy ML libraries for memory optimization
# Comment out these lines if you need to save maximum memory:
# spacy
# transformers  
# torch
# pandas
# scikit-learn
# camelot-py
# tabula-py
# paddleocr
# paddlepaddle

# Utilities
tqdm==4.66.1
