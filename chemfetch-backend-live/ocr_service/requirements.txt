# Ultra-lightweight requirements for Render Free (512MB limit)
# FIXED: Using exact versions that work together

# Build tools - keep these for safety
wheel
setuptools>=65.0.0

# Core Flask and web services
Flask==2.3.3
flask-cors==4.0.0
requests==2.31.0
gunicorn==21.2.0

# PDF text extraction - EXACT COMPATIBLE VERSIONS
pdfplumber==0.11.4  # Latest version
pdfminer.six==20231228  # Version that pdfplumber 0.11.4 actually requires
Pillow>=10.0.0  # Let pip choose a compatible version with pre-built wheels

# Text processing and parsing
python-dateutil==2.8.2
regex==2024.5.15

# Utilities (minimal)
numpy>=1.24.0,<2.0.0  # Flexible numpy version for better wheel availability

# REMOVED PROBLEMATIC PACKAGES:
# PyMuPDF - C++ compilation issues on Render
# paddleocr - MAIN MEMORY HOG (300-400MB)
# paddlepaddle - MAIN MEMORY HOG  
# opencv-python - Heavy image processing (50-100MB)
# pdf2image - OCR dependency (requires Poppler)
# pytesseract - OCR dependency (requires Tesseract)
# torch, transformers, spacy - ML libraries (100s of MB each)
# scikit-learn, pandas - Data science libraries
# camelot-py, tabula-py - Table extraction libraries

# TOTAL ESTIMATED MEMORY: ~80-120MB (much lighter without compilation)
