# Optimized requirements for Render Free Tier (512MB RAM, 0.5 CPU)
# Total estimated memory: ~150-200MB

# Essential build tools (minimal)
wheel
setuptools>=65.0.0

# Core Flask web framework (lightweight)
Flask==2.3.3
flask-cors==4.0.0
requests==2.31.0
gunicorn==21.2.0

# PDF text extraction - optimized versions for 512MB limit
# These work together without heavy dependencies
pdfplumber==0.11.4
pdfminer.six==20231228
Pillow>=10.0.0

# OCR support for scanned PDFs
pytesseract==0.3.13
pdf2image==1.17.0
PyMuPDF==1.26.4

# Text processing (essential only)
python-dateutil==2.8.2
regex==2024.5.15

# NumPy - minimal version for PDF processing
numpy>=1.24.0,<2.0.0

# MEMORY BREAKDOWN:
# - Flask + deps: ~20MB
# - PDF libraries: ~50MB  
# - NumPy: ~30MB
# - Python runtime: ~50MB
# - Total: ~150MB (comfortable within 512MB limit)

# This configuration provides:
# ✅ Reliable PDF text and image extraction
# ✅ OCR support for scanned SDS documents
# ✅ Fits within 512MB memory limit
# ✅ Fast startup and deployment
