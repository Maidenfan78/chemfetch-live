# OCR-enabled requirements for Docker/Render paid plans
# Lightweight OCR using Tesseract instead of PaddleOCR

# Core Flask and web services
Flask==2.3.3
flask-cors==4.0.0
requests==2.31.0
gunicorn==21.2.0  # Production WSGI server

# PDF text extraction (pure Python wheels)
PyMuPDF==1.23.5  # Primary PDF text extraction
pdfminer.six==20231228  # Fallback PDF text extraction

# Lightweight OCR stack (requires system dependencies)
pdf2image==1.16.3  # PDF to image conversion (requires Poppler)
pytesseract==0.3.10  # OCR engine (requires Tesseract)
Pillow==10.0.1  # Image processing

# Basic image processing (headless version saves memory)
opencv-python-headless==4.8.1.78
numpy==1.24.4

# Text processing and parsing
python-dateutil==2.8.2  # Date parsing for SDS documents
regex  # Advanced regex support

# MEMORY COMPARISON:
# Ultra-lightweight (text-only): ~100-150MB
# OCR-enabled (this file): ~200-300MB  
# Original PaddleOCR: ~400-500MB

# STILL REMOVED:
# paddleocr - Heavy ML model (300-400MB)
# paddlepaddle - Heavy ML framework
# torch, transformers, spacy - ML libraries
# scikit-learn, pandas - Data science libraries
